---
// Ruta `/` (ES): portada. Monta el Layout en español y le inyecta el contenido
// `Home`. `page="home"` resuelve título, meta, alternos hreflang y menú activo.
import Layout from '../layouts/Layout.astro';
import Home from '../components/pages/Home.astro';
---

<Layout lang="es" page="home">
  <Home lang="es" />
</Layout>
